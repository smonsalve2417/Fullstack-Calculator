services:
  backend:
    build:
      context: ./Go-Backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./Frontend
      args:
        VITE_API_URL: http://localhost:8080
    ports:
      - "5173:80"
